# Web Framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database
sqlalchemy==2.0.30
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9

# Pydantic
pydantic==2.7.1
pydantic-settings==2.2.1

# Auth & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3
python-multipart==0.0.9
cryptography==42.0.7

# LDAP / Active Directory (pure Python, no C extensions required)
ldap3==2.9.1

# SNMP (pure Python — lextudio-maintained fork, Python 3.11+ compatible)
# pysnmp 7.x requires pyasn1 >= 0.5.0; pin it explicitly to avoid old containers
# pulling the incompatible pyasn1 < 0.5.0 that still has pyasn1.compat.octets
pysnmp>=7.0.0
pyasn1>=0.5.0

# Redis with asyncio support (redis 5.x includes aioredis — no separate package needed)
redis[asyncio]==5.0.4

# HTTP Client
httpx==0.27.0

# Scheduling
apscheduler==3.10.4

# Utilities
netaddr==1.2.1
pytz==2024.1
python-dotenv==1.0.1
email-validator==2.1.1
aiofiles==23.2.1

# Rate limiting
slowapi==0.1.9
limits==3.12.0
asyncssh==2.14.2

# System monitoring
psutil>=5.9.0
